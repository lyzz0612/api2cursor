services:
  api2cursor:
    image: ghcr.io/lyzz0612/api2cursor:latest
    # 本地开发时注释掉 image 并取消下行注释，使用本地构建：
    # build: .
    container_name: api2cursor
    ports:
      - "${PROXY_PORT:-3029}:${PROXY_PORT:-3029}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:${PROXY_PORT:-3029}/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
